# Flags prepended to every google-chrome-stable launch.
# Sourced by the /usr/bin/google-chrome-stable wrapper, so this covers the
# launcher (Super+D / walker), Hyprland autostart, and terminal launches.
# One flag per line; lines starting with # are ignored.
--ozone-platform=wayland
--restore-last-session
